features : 
1. download the file in pdf formart so that even if local storage is removed the data can be protected since no account is used for sign up
   and also ability for copy the whole text as markdown. NOTE : both these features are to be implemented only for markdown
2. for creating pages and files mobile or smaller screens shouldnt be compatible(can be improved in future versions) but the shared pages maybe seen havent really decided on it yet
3. recently deleted....... do i have to ?🫠
4. authenticate
->. verify the necessasary files and folders in both ends and comments
->. remove the commented code from all the files
bug reports :
1. have to refresh everytime in the deployed version to see the changes
2. there are multiple userids being generated, have to see if they are legit or not
3. the name column in the User model is staying empty, can i use it or remove it
4. whats ownerId in the database "page" model
5. pages are taking so long to load (is that the problem with free tier)
6. search engine optimization along with ai search maximization
7. if the page is deleted from the database all the links attached to it will also be deleted from the database
8. the inactive links of live collaboration have to be removed from the database - does it have to be a cron job?
9. changing the title without any content is not happening - markdown alone
10. active session continuation
11. editing the task in kanban board in live collaboration, its not happening in real time
--------------------
live collaboration requirements : 
SENDER :
1. share modal pops up and select live collaboration option
2. instead of the just generating link, give an input field for inputing names or randomly generating the name(the input for name field should not visible for view or edit option)
3. the page tool bar should show the number of people participating(liveparticipants) and option to end the session so the link will become invalid, the end session button will not be present all the time on the toolbar but inside the share modal
4. while in a live collaboration, cursors should be present of different colors(assigned just like in excalidraw indicating presence of other members distinctively)

RECIEVER :
1. as soon as the link is pasted in the browser, a model has to pop up asking what name should be displayed
2. the reciever should be able to see the same contents of the page as the owner, with real time changes and display of live participants
3. reciever cant end the session, but even when the reciever goes offline the edits have to persist 
4. their presence and other people along with the owner who are present on the page should be shown like a coloured cursor
--------------------------------
--> rest of the features should not change and check the existing code before generating new lines of code
--------------------------------------
and when the person shares their page : 
1. choose the permissions to be allowed (view, comment[not available for now], and edit, live session)
2. copy the link and share
3. anonymous names / chosen names by the recieveing user, its like creating a live session in excalidraw
4. once the live session stops the link becomes invalid and cannot open the page for edit
5. creating only live session for editing permissions cause only to view no expiry (if that optimal)
6. maybe in the next versions we can add collaborators so that people can continuously have access for edits, comments and have them listed as contributors to the page
7. when multiple people are working on a page, their icons should be visible on the tool bar and their presense on the page has to be displayed
--------------------
backend structure : 

apps/server
│
├── src
│   ├── api
│   │   ├── documents
│   │   ├── share
│   │   └── permissions
│   │
│   ├── websocket
│   │   ├── rooms
│   │   ├── presence
│   │   └── yjs
│   │
│   ├── services
│   │   ├── documentService
│   │   ├── shareService
│   │   └── syncService
│   │
│   ├── db
│   │   ├── prisma
│   │   └── repositories
│   │
│   ├── middleware
│   │
│   └── utils
│
└── prisma
    └── schema.prisma

------========================----------------======================---------------------------------
Information for me : 
Backend - 
id is the primary key of the current table.

For example, in your ShareLink model:

model ShareLink {
  id        String @id @default(cuid())
  pageId    String
  token     String @unique
  access    AccessLevel

  page Page @relation(fields: [pageId], references: [id])
}

Here:

id → identifies a ShareLink
Every share link has its own unique id

Example:

ShareLink
---------
id      = sl_123
pageId  = page_456
token   = abcxyz
pageId

pageId is a foreign key.

It stores the ID of the page that this share link belongs to.

Page
----
id = page_456
title = "Meeting Notes"
ShareLink
---------
id      = sl_123
pageId  = page_456
token   = abcxyz

Notice:

sl_123 identifies the share link
page_456 identifies the page

===========================================================
used React portals here - sharemodal, have to dig deep into that along with dexie (indexedDB)

===============================================================
Phase 1 — Infrastructure
Install @hocuspocus/server, yjs, @tiptap/extension-collaboration, @tiptap/extension-collaboration-cursor, y-indexeddb, and y-websocket (or use Hocuspocus directly).
Create server/src/live/ and start a standalone Hocuspocus server.
Verify two browser tabs can edit the same test document in real time.
Phase 2 — Persistence
Implement onLoadDocument and onStoreDocument in Hocuspocus to load/save a page's Yjs state from PostgreSQL.
Keep your existing REST endpoints for page CRUD unchanged.
Phase 3 — Room management
Add liveRoutes, liveService, and liveRepository.
Support starting a live session, generating an invite link, joining by room ID, and ending the session.
Phase 4 — Frontend integration
Create features/live/ with hooks and services.
Add a Start Live Session button to PageToolbar.
Swap the normal editor for a collaborative editor when a live session is active.
Phase 5 — Presence
Use Yjs Awareness to display your randomly generated participant names, cursor colors, and live cursors.
Show a participant list and online count similar to Excalidraw.
Phase 6 — Polish
Add reconnect handling, offline support with y-indexeddb, snapshot throttling, session cleanup, and eventually features like shared undo/redo or comments.

============================================================================
One thing I'd improve in the schema

Since your UI allows:

Start Live Session -> End Session -> Start Again

I'd slightly modify LiveRoom.

Instead of

active Boolean

I'd use

status LiveRoomStatus
enum LiveRoomStatus {
    ACTIVE
    ENDED
}

Why?

Because later you might add:

ACTIVE
PAUSED
ENDED
EXPIRED

without another migration.

src/
├── live/
│   ├── provider.ts
│   ├── useLive.ts
│   ├── LiveContext.tsx
│   ├── awareness.ts
│   └── types.ts
│
├── pages/
│   ├── InvitePage.tsx
│   ├── EditorPage.tsx   (modified)
│   └── SharedPage.tsx   (modified)
│
├── components/
│   ├── LiveParticipants.tsx
│   ├── LiveStatus.tsx
│   ├── LiveToolbar.tsx
│   ├── JoinLiveModal.tsx
│   └── LiveSessionDialog.tsx
│
├── hooks/
│   ├── useLiveSession.ts
│   └── usePresence.ts
│
├── services/
│   └── liveApi.ts
│
├── types/
│   └── live.ts
│
├── editors/
│   ├── MarkdownEditor.tsx   (updated)
│   ├── ListEditor.tsx       (updated)
│   └── KanbanEditor.tsx     (updated)
│
└── routes/
    └── AppRoutes.tsx